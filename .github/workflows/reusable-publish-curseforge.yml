# Publishes build artifacts to CurseForge via the CurseForge Upload API (curl + jq, no 3rd-party action).
# The job is skipped when project-id is empty, so callers can make CurseForge optional per project.
name: reusable / publish-curseforge

on:
  workflow_call:
    inputs:
      project-id:
        description: Numeric CurseForge project id. Empty = skip.
        type: string
        default: ""
      api-base:
        description: "Per-game Upload API host (Java edition: https://minecraft.curseforge.com)"
        type: string
        default: https://minecraft.curseforge.com
      artifact-pattern:
        description: download-artifact pattern selecting the artifacts to publish.
        type: string
        required: true
      primary-file:
        description: Bash extglob (relative to the download dir) matching exactly ONE primary file.
        type: string
        default: "!(*-@(sources|dev|javadoc)).jar"
      additional-files:
        description: Bash extglob for child files attached to the primary one (e.g. *-sources.jar). Empty = none.
        type: string
        default: ""
      game-versions:
        description: Comma/newline separated CurseForge version names, e.g. "26.2,26.3,Fabric,Java 25,Client,Server".
        type: string
        required: true
      version-type-prefixes:
        description: Version-type slug prefixes to resolve names in. "" = all types.
        type: string
        default: minecraft,modloader,java,environment
      release-type:
        description: release | beta | alpha
        type: string
        default: release
      display-name:
        description: Display name on CurseForge (empty = file name).
        type: string
        default: ""
      relations:
        description: '"slug:type,..." e.g. "fabric-api:requiredDependency,modmenu:optionalDependency"'
        type: string
        default: ""
      changelog:
        description: Changelog markdown. Empty = body of the GitHub release for `tag`.
        type: string
        default: ""
      tag:
        description: Tag whose GitHub release body is used as changelog when `changelog` is empty.
        type: string
        default: ""
      dry-run:
        type: boolean
        default: false
    secrets:
      curseforge-token:
        description: CurseForge Upload API token (authors portal -> API tokens).
        required: false
    outputs:
      file-id:
        value: ${{ jobs.publish.outputs.file-id }}

permissions:
  contents: read

jobs:
  publish:
    if: inputs.project-id != ''
    runs-on: ubuntu-latest
    timeout-minutes: 15
    outputs:
      file-id: ${{ steps.upload.outputs.file-id }}
    steps:
      - uses: actions/download-artifact@v8
        with:
          pattern: ${{ inputs.artifact-pattern }}
          path: dist
          merge-multiple: true

      - name: Resolve changelog
        env:
          GH_TOKEN: ${{ github.token }}
          CHANGELOG: ${{ inputs.changelog }}
          TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          f="$RUNNER_TEMP/changelog.md"
          if [[ -n "$CHANGELOG" ]]; then
            printf '%s\n' "$CHANGELOG" > "$f"
          elif [[ -n "$TAG" ]] && gh release view "$TAG" --repo "$GITHUB_REPOSITORY" --json body --jq .body > "$f" 2>/dev/null && [[ -s "$f" ]]; then
            echo "Using body of GitHub release $TAG"
          else
            echo "See ${GITHUB_SERVER_URL}/${GITHUB_REPOSITORY}/releases/tag/${TAG}" > "$f"
          fi
          cat "$f"

      # BEGIN inlined scripts/curseforge-upload.sh (checked by scripts/check-inlined-script.sh)
      # A called workflow cannot read files of the repo that hosts it, so the script is inlined byte-for-byte.
      # Edit scripts/curseforge-upload.sh first, then paste it here (indented 10 spaces).
      - name: Write upload script
        run: |
          cat > "$RUNNER_TEMP/curseforge-upload.sh" <<'CF_UPLOAD_SCRIPT'
          #!/usr/bin/env bash
          # curseforge-upload.sh — upload one primary file (+ optional child files) to CurseForge
          # via the legacy "Upload API" (https://support.curseforge.com/.../9000197321-curseforge-upload-api).
          # Dependencies: bash, curl, jq (all preinstalled on ubuntu-latest runners).
          #
          # Usage: curseforge-upload.sh <primary-file> [additional-file ...]
          #
          # Environment:
          #   CF_TOKEN            (required) API token (Authors portal -> API tokens). Sent as X-Api-Token.
          #   CF_PROJECT_ID       (required) numeric project id.
          #   CF_API_BASE         default https://minecraft.curseforge.com  (per-game host; Bedrock: see report)
          #   CF_GAME_VERSIONS    names, comma/newline separated, e.g. "26.2,26.3,Fabric,Java 25,Client,Server"
          #   CF_TYPE_PREFIXES    comma list of version-type slug prefixes to search in.
          #                       default "minecraft,modloader,java,environment"; set to "" to search all types.
          #   CF_RELEASE_TYPE     release | beta | alpha                      (default release)
          #   CF_DISPLAY_NAME     display name of the primary file           (default: file name)
          #   CF_CHANGELOG        changelog text (or CF_CHANGELOG_FILE=path)
          #   CF_CHANGELOG_TYPE   markdown | text | html                     (default markdown)
          #   CF_RELATIONS        "slug:type,slug:type"  type in requiredDependency|optionalDependency|
          #                       embeddedLibrary|tool|incompatible          e.g. "fabric-api:requiredDependency"
          #   CF_DRY_RUN          "true" = resolve ids and print metadata, do not upload
          #   CF_VERSIONS_JSON / CF_TYPES_JSON  optional local JSON files instead of calling the API (testing)
          # Outputs (when $GITHUB_OUTPUT is set): file-id, file-ids (comma list)
          set -euo pipefail

          die() { echo "::error::$*" >&2; exit 1; }

          [[ $# -ge 1 ]] || die "usage: $0 <primary-file> [additional-file ...]"
          : "${CF_PROJECT_ID:?CF_PROJECT_ID is required}"
          [[ "$CF_PROJECT_ID" =~ ^[0-9]+$ ]] || die "CF_PROJECT_ID must be numeric (got '$CF_PROJECT_ID')"
          if [[ "${CF_DRY_RUN:-false}" != "true" ]]; then : "${CF_TOKEN:?CF_TOKEN is required}"; fi
          for f in "$@"; do [[ -f "$f" ]] || die "file not found: $f"; done

          API="${CF_API_BASE:-https://minecraft.curseforge.com}"; API="${API%/}/api"
          RELEASE_TYPE="${CF_RELEASE_TYPE:-release}"
          case "$RELEASE_TYPE" in release|beta|alpha) ;; *) die "CF_RELEASE_TYPE must be release|beta|alpha";; esac
          TYPE_PREFIXES="${CF_TYPE_PREFIXES-minecraft,modloader,java,environment}"

          cf_get() { # $1 = path
            curl -fsS --retry 4 --retry-all-errors --retry-delay 5 \
              -H "X-Api-Token: ${CF_TOKEN:-}" "$API$1"
          }

          tmp="$(mktemp -d)"; trap 'rm -rf "$tmp"' EXIT
          if [[ -n "${CF_VERSIONS_JSON:-}" ]]; then cp "$CF_VERSIONS_JSON" "$tmp/versions.json"; else cf_get /game/versions > "$tmp/versions.json"; fi
          if [[ -n "${CF_TYPES_JSON:-}" ]];    then cp "$CF_TYPES_JSON"    "$tmp/types.json";    else cf_get /game/version-types > "$tmp/types.json"; fi

          # --- resolve game version names -> ids -------------------------------------------------
          ids_json='[]'
          while IFS= read -r name; do
            name="$(echo "$name" | sed -e 's/^[[:space:]]*//' -e 's/[[:space:]]*$//')"
            [[ -z "$name" ]] && continue
            matches="$(jq -c --arg name "$name" --arg prefixes "$TYPE_PREFIXES" --slurpfile types "$tmp/types.json" '
                ($prefixes | split(",") | map(select(length > 0))) as $p
              | ($types[0] | map(select(($p | length) == 0 or (.slug as $s | any($p[]; . as $x | $s | startswith($x))))) | map(.id)) as $allowed
              | [ .[] | select((.name | ascii_downcase) == ($name | ascii_downcase))
                      | select(.gameVersionTypeID as $t | $allowed | index($t)) ]
              | map({id, name, gameVersionTypeID})' "$tmp/versions.json")"
            count="$(jq 'length' <<<"$matches")"
            if [[ "$count" -eq 0 ]]; then
              hint="$(jq -r --arg n "${name%%.*}" '[.[] | select(.name | startswith($n)) | .name] | unique | .[:15] | join(", ")' "$tmp/versions.json")"
              die "CurseForge game version '$name' not found (type prefixes: '${TYPE_PREFIXES}'). Similar: ${hint:-none}"
            fi
            if [[ "$count" -gt 1 ]]; then
              echo "::warning::'$name' matched $count versions ($(jq -c 'map(.id)' <<<"$matches")); using the highest id"
            fi
            id="$(jq 'max_by(.id).id' <<<"$matches")"
            echo "resolved '$name' -> $id"
            ids_json="$(jq -c --argjson id "$id" '. + [$id] | unique' <<<"$ids_json")"
          done < <(tr ',' '\n' <<<"${CF_GAME_VERSIONS:-}")

          [[ "$(jq length <<<"$ids_json")" -gt 0 ]] || die "no game versions given (CF_GAME_VERSIONS)"

          # --- changelog / relations -----------------------------------------------------------
          changelog="${CF_CHANGELOG:-}"
          if [[ -z "$changelog" && -n "${CF_CHANGELOG_FILE:-}" ]]; then changelog="$(cat "$CF_CHANGELOG_FILE")"; fi
          [[ -n "$changelog" ]] || changelog="No changelog provided."

          relations='[]'
          IFS=',' read -r -a rels <<<"${CF_RELATIONS:-}"
          for r in "${rels[@]}"; do
            r="$(echo "$r" | tr -d '[:space:]')"; [[ -z "$r" ]] && continue
            slug="${r%%:*}"; rtype="${r#*:}"; [[ "$rtype" == "$r" ]] && rtype=requiredDependency
            case "$rtype" in requiredDependency|optionalDependency|embeddedLibrary|tool|incompatible) ;;
              *) die "bad relation type '$rtype' for '$slug'";; esac
            relations="$(jq -c --arg s "$slug" --arg t "$rtype" '. + [{slug:$s, type:$t}]' <<<"$relations")"
          done

          # --- upload ---------------------------------------------------------------------------
          upload() { # $1 = file, $2 = metadata json
            if [[ "${CF_DRY_RUN:-false}" == "true" ]]; then
              echo "[dry-run] POST $API/projects/$CF_PROJECT_ID/upload-file  file=$1" >&2
              jq . <<<"$2" >&2; echo 0; return
            fi
            local resp code
            resp="$(curl -sS --retry 3 --retry-delay 10 -w '\n%{http_code}' \
              -H "X-Api-Token: $CF_TOKEN" \
              -F "metadata=$2;type=application/json" \
              -F "file=@$1" \
              "$API/projects/$CF_PROJECT_ID/upload-file")"
            code="${resp##*$'\n'}"; resp="${resp%$'\n'*}"
            [[ "$code" == 2* ]] || die "upload of $1 failed (HTTP $code): $resp"
            jq -er '.id' <<<"$resp" || die "unexpected response: $resp"
          }

          primary="$1"; shift
          meta="$(jq -nc \
            --arg changelog "$changelog" \
            --arg ctype "${CF_CHANGELOG_TYPE:-markdown}" \
            --arg name "${CF_DISPLAY_NAME:-$(basename "$primary")}" \
            --arg rtype "$RELEASE_TYPE" \
            --argjson gv "$ids_json" \
            --argjson rel "$relations" \
            '{changelog:$changelog, changelogType:$ctype, displayName:$name, gameVersions:$gv, releaseType:$rtype}
             + (if ($rel | length) > 0 then {relations:{projects:$rel}} else {} end)')"
          file_id="$(upload "$primary" "$meta")"
          echo "uploaded $primary -> file id $file_id"
          all_ids="$file_id"

          # child files (e.g. sources jar) are attached to the primary via parentFileID; they must NOT carry gameVersions
          for extra in "$@"; do
            child_meta="$(jq -nc --arg changelog "$changelog" --arg ctype "${CF_CHANGELOG_TYPE:-markdown}" \
              --arg rtype "$RELEASE_TYPE" --argjson parent "$file_id" --arg name "$(basename "$extra")" \
              '{changelog:$changelog, changelogType:$ctype, displayName:$name, parentFileID:$parent, releaseType:$rtype}')"
            cid="$(upload "$extra" "$child_meta")"
            echo "uploaded $extra -> file id $cid (child of $file_id)"
            all_ids="$all_ids,$cid"
          done

          if [[ -n "${GITHUB_OUTPUT:-}" ]]; then
            { echo "file-id=$file_id"; echo "file-ids=$all_ids"; } >> "$GITHUB_OUTPUT"
          fi
          CF_UPLOAD_SCRIPT
      # END inlined scripts/curseforge-upload.sh (checked by scripts/check-inlined-script.sh)

      - name: Upload to CurseForge
        id: upload
        working-directory: dist
        env:
          CF_TOKEN: ${{ secrets.curseforge-token }}
          CF_PROJECT_ID: ${{ inputs.project-id }}
          CF_API_BASE: ${{ inputs.api-base }}
          CF_GAME_VERSIONS: ${{ inputs.game-versions }}
          CF_TYPE_PREFIXES: ${{ inputs.version-type-prefixes }}
          CF_RELEASE_TYPE: ${{ inputs.release-type }}
          CF_DISPLAY_NAME: ${{ inputs.display-name }}
          CF_RELATIONS: ${{ inputs.relations }}
          CF_DRY_RUN: ${{ inputs.dry-run }}
          PRIMARY: ${{ inputs.primary-file }}
          ADDITIONAL: ${{ inputs.additional-files }}
        run: |
          set -euo pipefail
          shopt -s extglob nullglob
          export CF_CHANGELOG_FILE="$RUNNER_TEMP/changelog.md"
          # shellcheck disable=SC2206
          primary=( $PRIMARY )
          [[ ${#primary[@]} -eq 1 ]] || { echo "::error::primary-file '$PRIMARY' matched ${#primary[@]} files: ${primary[*]:-none}"; ls -la; exit 1; }
          extra=()
          # shellcheck disable=SC2206
          [[ -n "$ADDITIONAL" ]] && extra=( $ADDITIONAL )
          bash "$RUNNER_TEMP/curseforge-upload.sh" "${primary[0]}" "${extra[@]}"

      - name: Summary
        if: steps.upload.outputs.file-id != ''
        env:
          FILE_ID: ${{ steps.upload.outputs.file-id }}
          PROJECT_ID: ${{ inputs.project-id }}
        run: echo "### CurseForge upload OK — project $PROJECT_ID, file id $FILE_ID" >> "$GITHUB_STEP_SUMMARY"
